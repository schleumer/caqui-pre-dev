import React, { Component, PropTypes } from 'react'

class CaquiEditor extends Component {
  static propTypes = {
    onChange: PropTypes.func
  };

  render() {
    return <div>CaquiEditor</div>
  }
}

export default CaquiEditor
